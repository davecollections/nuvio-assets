{
  "name": "@nuvio/artwork-runtime-lookup",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic published-artwork runtime lookup builder and validator.",
  "type": "module",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "build": "node src/cli.mjs build",
    "validate": "node src/cli.mjs validate",
    "test": "node --test"
  }
}
