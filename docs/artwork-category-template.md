# Artwork category: CATEGORY NAME

Copy this template to docs/artwork-categories/CATEGORY-KEY.md and replace the placeholders. Keep it current as work progresses. A placeholder or proposed file is not publication evidence.

## Scope and identity

- Category key and display name:
- Owner request and date:
- Selected records and explicit exclusions:
- Identity authority and stable key rule:
- Content variants, if any (for example Movies, Series, Mixed):
- Repository/tool ownership and out-of-scope work:

## State and next action

- Current state: planned / sourcing / staged / reviewed / published
- Builder integration: absent / prepared / verified
- Nuvio acceptance: untested / owner-reviewed (record actual client and version if provided)
- Preexisting working changes to preserve:
- Last completed milestone:
- Next concrete action:
- Missing information or evidence:

## Design and delivery contract

| Asset role / variant | Required? | Dimensions | Delivery format | Stable target path |
| --- | --- | --- | --- | --- |
| Record only the roles this category needs | | | WebP for new delivery | Label uncreated targets as planned |

- Approved layout, typography, palette and framing:
- Real artwork source policy and cached evidence:
- Focus behaviour and matching base asset, if supplied:
- Hero/title-logo separation:
- Source acquisition, rendering and optimisation commands:
- Renderer/preset versions and font availability:
- Quality checks and size results:

## Manifest and builder contract

- Actual category manifest path, schema path and version (or clearly marked proposal):
- Stable identity / media variant / artwork-role mapping:
- Relationship to existing shared lookup or master index:
- Published URL and release commit, only after verification:
- Per-file hashes, byte counts and dimensions: reference the manifest or validation report.
- Builder consumer support and existing Nuvio field mapping:
- Missing roles and fallback behaviour; never invent an unpublished asset URL:

## Review and publication evidence

- Staging directory and proof/gallery:
- Source identity, URL/path and hash evidence:
- Exact-output review bindings: stable key, target, hash:
- Owner approval text/date and authorised scope:
- Tests and validation results:
- Planned or completed replacements, retained files and backups:
- Live URL verification and cache/versioning observations:
- Nuvio import JSON and contract validation, if requested:
- Final git status and any unrelated changes preserved:

## Recovery notes

- Which evidence is tracked, and which is ignored/local-only:
- Required cached source files and how to locate or legitimately reacquire them:
- Reproduction commands and expected outputs:
- Known limitations, failed attempts that affect the next decision, and unresolved choices:
- Do not regenerate, delete, publish, commit or push outside the owner's authorised scope.
